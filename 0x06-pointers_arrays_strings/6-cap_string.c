#include "main.h"
#include <stdio.h>

/**
 * isLower - determines whether ascii is lowercase
 *
 * @c: character
 *
 * Return: 1 if true, 0 if false
 */
int isLower(char c)
{
	return (c >= 97 && c <= 122);
}
